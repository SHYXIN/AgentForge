# 测试环境配置
APP_DEBUG=false
APP_LOG_LEVEL=WARNING

# 测试时使用内存存储
REPO_BACKEND=memory

# 数据库配置（测试时不使用）
DB_ENABLED=false
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=agentforge_test
